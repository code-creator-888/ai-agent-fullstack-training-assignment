# 仓库开发规范（通用，跨作业生效）

> **单一事实源**：Claude Code 经由 `CLAUDE.md` 的 `@AGENTS.md` 导入读取，Qoder 直接读取。
> 只改这一份，不要另建副本。
>
> **作业专属规则不在这里**，在各作业目录的 `AGENTS.md`（如 `1-1-assignment/AGENTS.md`）：
> 命令、测试数量、文件地图、该作业踩过的坑。**开工前两份都要读。**

本仓库是 AI Agent 全栈训练营作业集，每个作业一个顶层目录（`1-1-assignment/`、`2-x-...`…），
共用一个 `.venv` 与 `pyproject.toml`。

下面的规则来自真实踩过的坑，不是泛泛而谈——**每条 `规则` 后的 `反例` 都是本仓库实际发生过的 bug**。
反例中提到具体文件名（`provider.py` 等）的，都是 1-1 发生的事；**结论对新作业同样成立，不要重犯**。

---

## 0. 环境约定（用错命令会以为测试没跑）

```bash
uv sync                                   # 首次或依赖变更后
uv run python -m pytest <作业目录>/test_XXX.py -q
```

> ⚠️ **必须用 `uv run python -m pytest`**。裸 `uv run pytest` 会报 `Failed to spawn: pytest`
> （venv 里没有 pytest 控制台脚本）；直接 `python`/`python3` 也没有 pydantic。
> 作业目录内若有相对导入，**从仓库根目录执行**，不要在子目录里跑。

**本仓库未安装** ruff / mypy，不要声称跑过 lint 或类型检查。

**每个作业的具体命令目录、测试项数写在各自的 `AGENTS.md` 里**，不要把数字写进本文件——
数字属于作业，通用规则才属于仓库。

---

## 1. 参数透传：必须端到端验证到下游真实收到的值

**规则**：新增跨层参数时，不能只看函数签名对上了，必须实测"下游真正收到了什么"。
**签名里有个参数、但值是 `None`，是最隐蔽的失效。**

**反例（1-1 真实发生过两次，修了又犯）**：给 provider 传 `response_schema`，
而流式请求的该字段恒为 `None`，导致 provider 里整段能力协商成了**死代码**——
只有直接构造的测试能命中，真实 HTTP 请求永远走不到。第二次"修复"只是把参数**改名**，
问题原封不动。

**做法**：用 `**kw` 抓全量入参的探针实测，不要靠推理：

```python
captured = {}
async def fake(self, messages, **kw):        # **kw 是关键：抓全部入参
    captured.update(kw)
    yield ...                                 # 或 return ...

# 用真实请求对象驱动，而不是直接调 provider
print(captured)      # 亲眼确认每个字段都到位，而不是 None
```

**判定标准**：每个新加/改动的字段都是期望值。出现 `None`、缺失、与入参不一致，就是 bug。
**所有分支路径都要跑一遍**（1-1 的两次翻车都在流式路径）。

---

## 2. 同一功能的多条路径：必须共用同一份实现

**规则**：当一个功能有多条入口（流式/非流式、同步/异步、多个 provider 适配器）时，
**决策逻辑必须抽成共享方法**，不要让各路径各写一份靠人肉同步。

**反例**：某个 provider 的 `stream()` 接了参数却完全不用，而 `complete()` 做了——
两条路径行为分叉。**修法不是"把另一条也抄一遍"，而是抽成共享方法。抄一遍迟早再次分叉。**

**检查**：改任一分支后，跑对照探针，打印两条路径最终传给下游的参数，必须**逐字节一致**。

---

## 3. 测试：绝不能把错误行为断言成正确

**规则**：写断言前先问"这个行为符合需求吗"，而不是"当前代码返回什么"。
**把 bug 钉进测试比没测试更糟——它会阻止别人修复。**

**反例**：曾有 `assert req.response_schema is None` 断言某个约束模式的字段为 `None`，
让"约束完全失效"看起来像预期行为。**写测试时若发现自己在断言一个"似乎不太对"的行为，
先停下来确认需求，而不是照着当前输出写。**

**规则**：**覆盖度要落在对外接口层**（HTTP 端点 / CLI / 公共 API），而不只是内部函数的 Pydantic 校验。
曾有一整个测试类只做参数校验、没有一条真打端点，端点分流的 bug 全漏了。
用 `TestClient`（或等价方式）打真实入口：

```python
from fastapi.testclient import TestClient
client = TestClient(app)
resp = client.post("/v1/llm", json={...})
assert resp.status_code == 200
assert "text/event-stream" in resp.headers["content-type"]
```

**规则**：**改了 Web 框架的 schema 声明（`response_model` / `responses` / 路由装饰器）后，
必须实测 API 文档能生成**。曾有写法错误导致 `GET /openapi.json` 直接 **500**、文档 UI 空白，
而当时几十项测试**全绿**——没有一条测试碰过它。**每个 Web 作业都应有一条守卫测试**：

```python
assert TestClient(app).get("/openapi.json").status_code == 200
```

**规则**：**mock 签名必须与真实签名逐字一致**。演进时最容易被漏掉的是"没报错的那份 mock"。
改任何被 mock 的方法签名后，全仓搜索确认没有漂移：

```bash
grep -rn "def fake_\|def mock_\|def always_" <作业目录>/
```

---

## 4. 语义契约：时间预算与计数

**规则**：若某个时间参数是**全局预算**（如整个重试/降级链共享的总超时），
就必须**递减地**传给下游 `min(总预算, deadline - now)`，且**所有路径都要**。
不能原样透传——否则最坏耗时是 `路径数 × 重试数 × 单次超时`，远超调用方预期。

**验证**：用极短预算 + 必然超时的 fake，打印下游每次收到的超时值。
**期望：递减序列，且每一项 ≤ 预算。**（1-1 实测参考：`[0.3, 0.132, 0.111]`）

**规则**：计数器（如 `attempts`）只统计**真正发起了调用**的尝试。
自增语句必须在预算检查**之后**——曾出现"预算耗尽 break 但仍计一次"导致审计虚高。
**验证方法：数 fake 被调用的次数，必须与记录的计数严格相等。**

**规则**：审计/计量记录（token、成本、耗时）必须与实际一致。
**异常路径（如流中断）也要带上已收集的数据**，不能让统计归零。

---

## 5. Pydantic：常量放类外，绝不放字段区之后

**规则**：Pydantic v2 中，写在**字段声明之后**的"类变量"会被当作**私有属性**收进
`__private_attributes__`，语义含糊。**首选放在类定义之前的模块级常量**：

```python
ALLOWED_TYPES: frozenset[str] = frozenset({"a", "b"})

class MyModel(BaseModel):
    field: str
```
（若必须放类内，则放字段区**之前**并标注 `ClassVar`。）

**规则**：输入模型保持 `extra="forbid"`（防注入）。**不要为了图方便关掉。**

---

## 6. 接口契约变更要显式确认兼容性

**规则**：改对外行为时，逐个确认并留下实测证据：

- **旧调用方是否受影响**（实际跑一次旧用法）
- **行为变更是否有意为之，且写进文档**（如某组合从"静默忽略"变成 422 拒绝）
- **API 文档是否还准确**（见第 3 节的守卫测试）
- **错误响应格式是否一致**：该返回 JSON 的错误不能退化成别的形式
  （如流式端点里，鉴权/限流/校验错误必须仍是 JSON，不能变成流内嵌的 error 事件）

---

## 7. 文档同步（本仓库反复漂移的地方）

**改代码时同一次提交内更新对应文档**，不要攒成专门的"文档对齐"提交。

| 改动 | 必须同步 |
|------|---------|
| 新增/修改入参字段 | 设计文档的模型代码块 + README 的调用示例 |
| 对外行为变更 | 设计文档的端点/接口表 + 示例 |
| **测试数量变化** | 设计文档的测试项数 **+ 该作业的 `AGENTS.md`** |
| 消除某条已知限制 | 设计文档的「已知限制」表 **+ 该作业的 `AGENTS.md`** |
| 行为收窄/拒绝新取值 | README 的注意事项段落 |

**反向核对**：文档里提到的文件必须真实存在（曾列过实际不存在的 `requirements.txt`）。

**规范本身也会过期**：改完代码回头核对各 `AGENTS.md` 中的数字与遗留项——
历史上出现过"遗留项其实早已修复、规范还在教人绕坑"。

---

## 8. 提交前 Checklist（通用部分；作业专属项见各作业 `AGENTS.md`）

- [ ] 该作业的测试全绿，e2e collect 正常（**命令见作业 `AGENTS.md`**）
- [ ] 新参数已实测到下游真实入参，非 `None`，**所有路径都跑了**
- [ ] 多路径功能行为一致，且决策逻辑是**共享实现**
- [ ] 新增断言钉的是正确行为；对外接口层有覆盖；API 文档守卫测试通过
- [ ] 时间预算递减、计数器准确、审计计量完整
- [ ] 文档已同步，无引用不存在的文件
- [ ] `git diff` 自查：无残留调试 `print` / 注释掉的代码

---

## 9. 提交信息风格

沿用 `feat: <主题> — <要点>` / `fix:` / `docs:`，正文用中文分块（核心修复 / 测试增强 / 清理），
列出关键变更与**测试数量变化**。参考 `git log -3` 里已有的提交。

---

## 10. 新增作业时

1. 建 `<编号>-assignment/AGENTS.md`，照 `1-1-assignment/AGENTS.md` 的结构写：
   命令与测试数、文件地图、该作业的坑、语义契约实测值、专属 Checklist、已知遗留。
2. **把本文件第 1–7 节重新审一遍**：若新作业引入了新的通用坑，补充到本文件；
   若某条只对 1-1 成立（如 `provider.py` 相关细节），**留在作业文件里，不要污染通用层**。
3. 本文件**不记录数字**（测试项数、行数等），数字一律归作业文件。
